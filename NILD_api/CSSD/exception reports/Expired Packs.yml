info:
  name: Expired Packs
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:3000/api/cssd/exception/expired
  headers:
    - name: Authorization
      value: Bearer TOKEN
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
