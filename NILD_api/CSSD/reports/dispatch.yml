info:
  name: dispatch
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:3000/api/cssd/reports/dispatch
  headers:
    - name: Authorization
      value: Bearer TOKEN
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
