info:
  name: report-stats
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:3000/api/cssd/reports/report-stats
  headers:
    - name: Authorization
      value: Bearer TOKEN
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
