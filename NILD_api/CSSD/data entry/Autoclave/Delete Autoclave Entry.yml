info:
  name: Delete Autoclave Entry
  type: http
  seq: 3

http:
  method: DELETE
  url: http://localhost:8080/api/cssd/data-entry/autoclave/1
  headers:
    - name: Authorization
      value: Bearer YOUR_AUTH_TOKEN
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
