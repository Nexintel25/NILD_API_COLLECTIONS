info:
  name: Machine Biological Indicator Result (Moves to Sterile Inventory)
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:3000/api/cssd/workflow/sterilization/bi-result
  headers:
    - name: Authorization
      value: Bearer TOKEN
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "autoclaveId": "AC-01",
        "batchNumber": "BATCH-260301-01", 
        "result": "pass",
        "shelfLocation": "Rack B, Shelf 2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
