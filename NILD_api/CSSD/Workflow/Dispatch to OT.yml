info:
  name: Dispatch to OT
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:3000/api/cssd/workflow/sterile/dispatch
  headers:
    - name: Authorization
      value: Bearer TOKEN
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "executionId": "1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
