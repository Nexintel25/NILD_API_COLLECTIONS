info:
  name: autoclave instrumentlist
  type: http
  seq: 1

http:
  method: post
  url: http://localhost:5000/data-entry/autoclave
  headers:
    - name: Authorization
      value: Bearer YOUR_JWT_TOKEN
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "setId": 12,
        "departmentId": 8,
        "departmentName": "Operation Theatre",
        "loadType": "Surgical Instruments"
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
