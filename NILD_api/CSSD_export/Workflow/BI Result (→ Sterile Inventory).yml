info:
  name: BI Result (→ Sterile Inventory)
  type: http
  seq: 5

http:
  method: POST
  url: http://192.168.1.112:5000/api/cssd/workflow/sterilization/bi-result
  headers:
    - name: Authorization
      value: Bearer YOUR_BEARER_TOKEN
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "autoclaveId": "AC-01",
        "batchNumber": "BATCH-260301-01",
        "result": "pass",
        "shelfLocation": "Rack B, Shelf 2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
