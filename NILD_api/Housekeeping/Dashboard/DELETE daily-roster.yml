info:
  name: DELETE daily-roster
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:5000/api/housekeeping/dashboard/daily-roster/:id
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
