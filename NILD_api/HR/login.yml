info:
  name: login
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:5000/api/auth/login
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "tenant_id": 1,
          "identifier": "user_hr",
          "password": "Admin@123"
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
