info:
  name: Update cycle status
  type: http
  seq: 9

http:
  method: POST
  url: http://localhost:5000/api/hr/payroll/cycles/1/update-status
  headers:
    - name: Authorization
      value: Bearer $TOKEN
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: '{"status":"processing"}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
