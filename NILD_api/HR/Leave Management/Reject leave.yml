info:
  name: Reject leave
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:5000/api/hr/leave/requests/11/reject
  headers:
    - name: Authorization
      value: Bearer $TOKEN
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: '{"remarks":"Insufficient leave balance"}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
