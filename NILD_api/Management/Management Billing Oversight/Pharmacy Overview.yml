info:
  name: Pharmacy Overview
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/management/billing/pharmacy-overview?page=1&limit=15&startDate={{startDate}}&endDate={{endDate}}&payment_mode=all"
  params:
    - name: page
      value: "1"
      type: query
    - name: limit
      value: "15"
      type: query
    - name: startDate
      value: "{{startDate}}"
      type: query
    - name: endDate
      value: "{{endDate}}"
      type: query
    - name: payment_mode
      value: all
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Pharmacy sales overview with aggregates and items.
