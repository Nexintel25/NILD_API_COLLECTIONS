info:
  name: Bill Detail
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/management/billing/bills/{{billId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Single bill full detail with items and payments.
